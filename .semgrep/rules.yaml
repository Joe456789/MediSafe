rules:
  - id: detect-hardcoded-google-api-key
    pattern-regex: 'AIzaSy[A-Za-z0-9_\\-]{33}'
    message: "Security Issue: Hardcoded Google API key prefix detected. Do not check credentials into source control. Move secrets to .env file."
    languages:
      - python
    severity: ERROR
